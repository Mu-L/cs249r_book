concept_map:
  source: nn_architectures.qmd
  generated_date: 2026-05-21
  primary_concepts:
    - Architecture as infrastructure and hardware contract
    - Inductive bias and data-structure matching
    - Architecture families and system bottlenecks
    - Arithmetic intensity and data movement
    - Deployment-constrained architecture selection
  secondary_concepts:
    - MLP dense connectivity and universal approximation limits
    - CNN spatial locality, parameter sharing, and translation equivariance
    - RNN hidden-state recurrence and sequential bottlenecks
    - Attention QKV routing and quadratic sequence scaling
    - Transformers as parallel self-attention architectures
    - DLRM sparse embeddings and memory-capacity limits
    - Shared building blocks: skip connections, normalization, gating
    - Lighthouse workloads: ResNet-50, GPT-2, DLRM, MobileNet, KWS
  technical_terms:
    - GEMM
    - im2col
    - Depthwise separable convolution
    - Receptive field
    - Multi-head attention
    - Positional encoding
    - KV cache
    - Embedding table
    - Embedding sharding
    - All-to-all communication
    - FlashAttention
    - Tensor Cores
  methodologies:
    - Pattern needs -> algorithmic structure -> computational mapping -> system implications analysis
    - Data-to-architecture mapping
    - Arithmetic intensity and workload signature analysis
    - Architecture selection decision framework
    - Hardware mapping and optimization strategy selection
    - Production constraint evaluation for memory, compute, latency, accuracy, and hardware fit
  formulas:
    - 'Dense layer: $h^{(l)} = f(h^{(l-1)}W^{(l)} + b^{(l)})$'
    - 'Convolution: $output[i,j,k] = \sum_c \sum_{di,dj} W[di,dj,c,k] X[i+di,j+dj,c]$'
    - 'Translation equivariance and invariance: $f(T(x)) = T(f(x))$ vs. $f(T(x)) = f(x)$'
    - 'RNN update: $h_t = f(W_{\text{hh}}h_{t-1} + W_{\text{hx}}x_t + b_h)$'
    - 'Scaled attention: $Attention(Q,K,V) = softmax(QK^T / \sqrt{d_k})V$'
    - 'Self-attention: $SelfAttention(X) = softmax(XW_Q(XW_K)^T / \sqrt{d_k})XW_V$'
    - 'Residual block: $y = F(x) + x$'
    - 'KV cache scaling: $O(batch \times layers \times 2 \times heads \times seq\_len \times head\_dim)$'
