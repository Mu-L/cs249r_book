concept_map:
  source: training.qmd
  generated_date: 2026-05-21
  primary_concepts:
    - Training Cost Asymmetry
    - Iron Law of Training Performance
    - Training Pipeline Architecture
    - Training Bottleneck Diagnosis
    - Memory Capacity and Activation Storage
    - Optimizer State Trade-offs
    - Mixed-Precision Training
    - IO-Aware Attention Optimization
    - Gradient Accumulation and Activation Checkpointing
    - Multi-GPU Scaling and Communication Tax
  secondary_concepts:
    - GPT-2 Lighthouse Training Example
    - Accelerator Bubbles
    - Matrix-Multiply-Dominated Training
    - Batch Size, Critical Batch Size, and Linear Scaling Rule
    - Model FLOPs Utilization
    - Arithmetic Intensity and Roofline Regimes
    - Data Prefetching and Pipeline Overlap
    - Precision Format Selection
    - Tensor Core Hardware Alignment
    - Attention Memory Bottleneck
    - Data vs. Model Parallelism
    - Pipeline and Tensor Parallelism
    - Scaling Physical Ceilings
    - Training Energy and Carbon Cost
  technical_terms:
    - Forward Pass
    - Backward Pass
    - Stochastic Gradient Descent
    - Adam and AdamW
    - Optimizer State
    - Activation Memory
    - Loss Scaling
    - Gradient Clipping
    - FP16, BF16, and FP8
    - High-Bandwidth Memory
    - On-Chip SRAM
    - AllReduce and Ring AllReduce
    - NVLink and InfiniBand
    - Micro-batch
    - Wave Quantization
  methodologies:
    - Profile-select-compose-reprofile optimization
    - D-A-M bottleneck classification
    - Roofline analysis
    - Data prefetching and overlapping
    - Automatic mixed precision with loss scaling
    - IO-aware attention tiling
    - Gradient accumulation over micro-batches
    - Selective activation checkpointing
    - Learning rate warmup and cosine decay
    - Parallelism strategy selection
  formulas:
    - "Training time: O / (R_peak * eta_hw)"
    - "Model FLOPs Utilization: C_model / (R_peak * T_step)"
    - "Arithmetic intensity: FLOPs / bytes moved"
    - "Training memory: weights + gradients + optimizer state + activations"
    - "Activation memory per batch: B * sum_l(s_l + a_l)"
    - "System throughput: min(R_pipeline, R_compute)"
    - "Overlapped iteration time: max(t_fetch, t_process, t_transfer)"
    - "Effective batch size: micro-batch * accumulation steps * devices"
    - "Attention: softmax(QK^T / sqrt(d_k))V"
    - "Checkpoint memory: kA + (L/k)A with k_opt = sqrt(L)"
    - "Communication-tax speedup: N / (1 + (N - 1)r)"
